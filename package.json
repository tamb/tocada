{
  "name": "tocada",
  "version": "2.3.0",
  "description": "Touch events with ease! (Inspired by the wonderful Tocca.js library). Written in Typescript with love",
  "main": "dist/index.js",
  "types": "dist/index.d.ts",
  "exports": {
    ".": {
      "import": "./dist/index.js",
      "require": "./dist/index.js",
      "types": "./dist/index.d.ts"
    }
  },
  "directories": {
    "test": "test"
  },
  "files": [
    "dist"
  ],
  "scripts": {
    "build": "tsc --emitDeclarationOnly --declaration --outDir ./dist && bun build.js",
    "build:pack": "bun run build && npm pack",
    "build:watch": "concurrently \"tsc --watch\" \"bun build.js --watch\"",
    "demo": "bun clean.ts && bun run test && bun run build && npm pack && bun run demo:gh-pages",
    "demo:gh-pages": "cd gh-pages && bun install --clean && bun run dev:local",
    "safe-publish": "bun run test && bun run build && npm publish",
    "serve": "bunx serve ./",
    "test": "bun test",
    "test:watch": "bunx test --watch"
  },
  "repository": {
    "type": "git",
    "url": "git://github.com/tamb/tocada.git"
  },
  "author": "Gianluca Guarini, Tamb",
  "license": "MIT",
  "bugs": {
    "url": "https://github.com/tamb/tocada/issues"
  },
  "homepage": "https://github.com/tamb/tocada.js",
  "keywords": [
    "touch",
    "events",
    "mobile",
    "swipe",
    "gesture",
    "tap",
    "dbltap",
    "longtap",
    "swipeleft",
    "swiperight",
    "swipeup",
    "swipedown",
    "swipecancel",
    "velocity",
    "pinch",
    "spread",
    "typescript"
  ],
  "devDependencies": {
    "@happy-dom/global-registrator": "^20.0.10",
    "concurrently": "^9.2.1",
    "typescript": "^5.9.3"
  }
}